import { BusyOverlayDirective } from './busy-overlay.directive';

describe('BusyOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new BusyOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
