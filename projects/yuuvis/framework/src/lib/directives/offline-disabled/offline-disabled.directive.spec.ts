import { OfflineDisabledDirective } from './offline-disabled.directive';

describe('OfflineDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new OfflineDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
