import { FocusFirstDirective } from './focus-first.directive';

describe('FocusFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusFirstDirective();
    expect(directive).toBeTruthy();
  });
});
