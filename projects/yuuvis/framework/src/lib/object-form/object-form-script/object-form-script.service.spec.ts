// import {TestBed, inject} from '@angular/core/testing';
// import {ObjectFormScriptService} from './object-form-script.service';

describe('ObjectFormScriptService', () => {
  /*
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ObjectFormScriptService]
    });
  });

  it('should ...', inject([ObjectFormScriptService], (service: ObjectFormScriptService) => {
    expect(service).toBeTruthy();
  }));
  */
});
